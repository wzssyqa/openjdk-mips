os_family = linux

arch = mips

arch_model = mips_32

os_arch = linux_mips

os_arch_model = linux_mips_32

lib_arch = mips

compiler = gcc

gnu_dis_arch = mips

sysdefs = -DLINUX -D_GNU_SOURCE -DMIPS32
